import React from 'react';
import { motion } from "framer-motion";

const Home = () => {
    return (
        <motion.div
            initial="initial"
            animate="in"
            exit="out"
        >
            
        </motion.div>
    );
};

export default Home;